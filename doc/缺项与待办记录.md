# 智能充电桩 APP — 缺项与待办记录

> 记录当前尚未实现 / 以降级方式实现的功能点，供后续迭代与后端联调对照。
> 变更记录见 git 提交历史。本文档随开发滚动更新。

当前整体状态：**登录 → 附近电站（一级）→ 充电中心详情（二级）→ 电枪详情（三级）** 已打通，数据全部为本地模拟。

---

## 一、功能缺项（按 Story 对照）

| Story | 内容 | 状态 | 缺项说明 |
|-------|------|------|----------|
| 1 | 用户注册 | ⬜ 未开始 | 未建 `Register.ets`；注册页、协议勾选、账号唯一性校验均无 |
| 2 | 用户登录 | 🟡 半成品 | 仅做非空校验 + 跳转；无真实鉴权（无后端） |
| 3 | 底部导航栏 | 🟡 降级 | 目前 `Index` 为主页直接串联三级页面，**未实现 `Tabs` 五栏**（首页/充电/扫一扫/订单/我的） |
| 4 | 查看当前充电状态 | ⬜ 未开始 | 首页卡片（未充电/充电中）未实现 |
| 5 | 附近充电站快捷入口 | ⬜ 未开始 | 首页入口未实现 |
| 6 | 充电中心列表（含地图） | ✅ 已完成 | 自绘示意地图 + 最近大卡 + 推荐排行 |
| 7 | 充电中心详情 | ✅ 已完成 | 大图、占用/空闲、收费信息、桩列表 |
| 8 | 充电桩详情 | ✅ 已完成 | 车牌号、费用、接线示意、立即充电（占位） |
| 9 | 扫码充电 | ⬜ 未开始 | 无扫码；需相机/ScanKit，待后端+权限 |
| 10 | 启动充电确认 | 🟡 占位 | 「立即充电」仅 toast 提示，**确认弹窗（预估费用+规则）未实现** |
| 11 | 充电实时状态 | ⬜ 未开始 | 电压/电流/功率/时长/电量/费用实时刷新未实现 |
| 12 | 结束充电 | ⬜ 未开始 | 结束→生成订单未实现 |
| 13 | 结束汇总 | ⬜ 未开始 | 汇总（电量/时长/费用）未实现 |
| 14 | 订单列表 | ⬜ 未开始 | `OrderView` 未建 |
| 15 | 订单详情 | ⬜ 未开始 | 未建 |
| 16 | 个人信息 | ⬜ 未开始 | `MineView` 未建 |

---

## 二、降级 / 技术债项

| 项目 | 现状 | 理想方案 |
|------|------|----------|
| 地图 | **自绘 Canvas 示意地图**（道路/公园/水域 + 站点标记） | 替换为 MapKit / 高德 / 天地图（需申请 Key，配置在环境变量，**不入仓库**） |
| 站点/桩图片 | 渐变色块 + 图标占位 | 后端返回真实图片 URL |
| 充电数据 | 静态模拟（`MockData`） | 后端实时推送 / WebSocket |
| 用户体系 | 无；登录仅前端校验 | 后端真实注册/登录 + Token 鉴权 + 密码加密 |
| 桩状态 | `IDLE/CHARGING/OFFLINE` 仅静态展示 | 对接实时状态 |
| 定位 | 站点距离为写死常量 | 真实定位（`@kit.Geolocation`，需权限） |
| 明文密码 | 代码中出现 `password` 字段（演示用） | 后端鉴权，前端不落明文 |

> ⚠️ 密码等敏感字段仅作前端演示。**严禁**将真实密钥/Token/内网地址写入代码或提交仓库。

---

## 三、待后端联调清单

1. 注册 / 登录接口、Token 鉴权、用户信息接口。
2. 附近充电中心列表、充电中心详情、充电桩详情接口（替换 `MockData`）。
3. 启动充电 / 结束充电 / 充电订单生成接口。
4. 充电实时状态推送（WebSocket 或轮询）。
5. 充电订单列表、订单详情接口。
6. 定位、扫码（相机权限 + ScanKit）。

---

## 四、近期建议开发顺序

1. **Story 3 底部 `Tabs` 导航**：让「充电」tab 承载现有三级页面，首页/订单/我的先用空页占位。
2. **Story 10 启动确认弹窗**：接「立即充电」按钮，纯前端弹窗。
3. **Story 11 充电中实时状态**：`ChargeSimulator` 定时器模拟刷新（无需后端）。
4. 后端就绪后再批量替换数据源为真实接口。
